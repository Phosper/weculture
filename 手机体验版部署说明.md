# Weculture 手机体验版部署说明

队友在各自手机测试时，推荐使用“微信云托管 + 云 MySQL + 小程序体验版”。小程序通过 `wx.cloud.callContainer` 访问后端，不需要另外购买传统服务器，也不需要为体验测试配置 `request` 合法域名。

## 1. 创建测试后端

1. 使用本项目 AppID `wxdc3bf459aac6b500` 登录微信云托管控制台并创建环境。
2. 创建名为 `weculture-api` 的服务，代码目录选择 `weculture-api`，构建方式选择 Dockerfile，容器端口填写 `3000`。
3. 在同一地域创建测试用 MySQL 8 数据库，数据库字符集使用 `utf8mb4`。
4. 为服务配置以下环境变量。密码和密钥只填写在云控制台，不要提交到 Git：

   - `NODE_ENV=production`
   - `PORT=3000`
   - `DB_TYPE=mysql`
   - `DB_HOST`、`DB_PORT`、`DB_USERNAME`、`DB_PASSWORD`、`DB_DATABASE`
   - `DB_MIGRATIONS_RUN=true`
   - `DB_SSL=false`（同 VPC 内网数据库；公网数据库按服务商要求配置）
   - `JWT_SECRET`（至少 32 位随机字符串）
   - `WECHAT_APP_ID=wxdc3bf459aac6b500`
   - `WECHAT_APP_SECRET`（从小程序后台获取，只保存在云控制台）
   - `CORS_ORIGINS`（仅部署浏览器运营后台时填写其 HTTPS 地址；只测小程序可不填）
   - `SEED_DEMO_DATA=true`（首次测试部署使用）
   - `ADMIN_INITIAL_PASSWORD`、`OPERATOR_INITIAL_PASSWORD`、`REVIEWER_INITIAL_PASSWORD`
   - `SCHOOL_INVITE_CODE`（提供给测试队友的学校邀请码）

5. 部署后访问服务健康检查 `/api/v1/health`，确认返回 `code: "OK"`。
6. 演示数据初始化完成后，将 `SEED_DEMO_DATA` 改为 `false` 并重新部署，避免重启时重复执行初始化逻辑。

## 2. 连接小程序体验版

1. 在微信云托管控制台复制环境 ID。
2. 编辑 `weculture-miniprogram/config.js`，把 `cloudContainers.trial.env` 填为该环境 ID；如果服务没有使用默认名称，同时修改 `service`。
3. 在微信开发者工具中确认基础库不低于 2.13.1，编译并完成一轮登录、认证和内容浏览检查。
4. 点击“上传”，填写体验版版本号和说明。

## 3. 邀请队友测试

1. 登录微信公众平台，在“成员管理”中把队友微信号添加为体验成员。
2. 在“版本管理”的开发版本中选中刚上传的版本，设为体验版。
3. 把体验版二维码发给队友，并同时发送 `队友功能测试说明.md` 和测试邀请码。

## 4. 测试结束

导出并保留需要的问题数据，然后停掉云托管实例或将最小实例数调为 0，避免继续产生资源费用。测试账号、数据库和正式环境必须隔离。
