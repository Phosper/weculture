# Weculture 小程序需求说明

## 1. 文档目的

本说明以现有 Weculture 演示 demo 为参考，重新定义一套一致的小程序产品范围和实现边界。后续代码将从零开始搭建，不直接迁移 demo 中分散的页面代码；demo 中保留的内容仅作为功能、文案和视觉方向的参考。

本期目标是完成一套可演示、可扩展的微信原生小程序与独立后端：用户能发现文化内容、浏览和参与社区活动、发布内容、生成并收藏文化旅行路线、查看个人主页，并通过“校园小C”进入校园服务问答。

## 2. 产品定位

Weculture 面向高校学生、国际学生与文化爱好者，连接城市文化、校园生活和跨文化交流。

产品提供以下价值：

- 用内容流帮助用户发现文化活动、非遗体验和校园故事。
- 用分圈社区承载同城活动、国际交流和校园互助。
- 用活动报名和旅行路线将“看内容”转化为可执行的线下计划。
- 用个人主页沉淀用户的文化身份、笔记、收藏和路线。
- 用校园小C提供课表、活动、失物招领、搭子和餐饮等快捷服务入口。

## 3. 首期信息架构

小程序采用五栏底部导航，保持所有页面入口与返回逻辑一致。

| 导航 | 页面 | 首要任务 |
| --- | --- | --- |
| 首页 | 发现 | 浏览文化内容、切换内容圈层、搜索、进入校园小C |
| 社区 | 社区 | 浏览同城/国际/校园动态，点赞、报名、发布 |
| 发布 | 发布页 | 发布动态、活动或二手物品 |
| 旅行 | 文化旅行 | 填写出行偏好，生成、查看并加入文化路线 |
| 我的 | 我的主页 | 查看个人资料、笔记、收藏、已加入路线 |

“校园小C”不是底部导航主栏，作为首页卡片、校园圈入口和个人服务入口可进入的二级页面。

## 4. 核心用户与场景

### 4.1 校园学生

用户想加入校园社团、寻找活动伙伴、解决校园网络等日常问题，或进行二手交换。

### 4.2 国际学生与跨文化交流者

用户想了解本地城市文化、参加语言交换或结识不同国家的同学。

### 4.3 文化爱好者

用户想发现非遗、建筑、茶艺、展览等内容，并按偏好生成短途文化旅行计划。

## 5. 页面与功能说明

### 5.1 首页：发现

**目标**：帮助用户快速发现感兴趣的文化内容和服务入口。

**页面组成**：

- 顶部品牌区：Weculture 标识及小程序标准胶囊按钮。
- 圈层切换：同城、国际圈、校园圈；切换后刷新对应内容流。
- 搜索框：支持按内容标题、用户昵称、话题进行本地模拟搜索；首期不要求完整搜索历史或联想词。
- 校园小C快捷卡：进入 AI 助手页面。
- 瀑布或双列内容流：展示封面、作者、学校、文化标签、标题、点赞量和分享入口。
- 内容操作：点赞状态切换；点击卡片进入内容详情页（首期可使用通用详情页）。

**示例内容标签**：文化大使、文化传承、非遗工坊、古建探秘。

### 5.2 社区

**目标**：让用户围绕附近、国际交流和校园生活产生互动。

**页面组成**：

- 圈层切换：同城动态、国际圈动态、校园圈动态。
- 圈层说明与活动提示：例如同城范围、校园名称、近期活动数。
- 筛选：全部动态、活动与报名、求助与互助。
- 动态列表：作者、身份/学校、标签、标题、正文、图片、点赞、评论和分享。
- 活动卡片：日期、名称、集合/地点、报名按钮。
- 发布入口：弹出“动态 / 活动 / 二手”三种发布类型，再进入发布页。

**首期交互规则**：

- 点赞可切换并更新数值。
- 报名按钮在“报名”和“已报名”之间切换；已报名活动同步显示在“我的-路线/计划”中。
- 评论与分享首期以提示反馈或底部面板占位呈现，不实现真实评论链与分享回流。

### 5.3 校园圈专题页

**定位**：这是社区“校园圈”的强化入口，可作为二级页保留，而非再占用一个底部导航位。

**页面内容**：

- 分类快捷入口：互助、市集、交友、社团、公告。
- 置顶互助帖与解决方案摘要。
- 二手物品卡片：价格、成色、交易方式、意向按钮。
- 校园活动卡片：时间、地点、报名人数、报名状态。

**说明**：首期也可以先将这些内容统一收进“社区-校园圈”，待产品验证后再决定是否单独开放专题页。

### 5.4 发布页

**目标**：用一套表单完成三种社区内容的创建，避免 demo 中发布入口不一致的问题。

**发布类型**：动态、活动、二手。

**共同字段**：

- 标题：最多 40 字，必填。
- 正文：必填。
- 发布圈层：同城、国际圈、校园圈，必选。
- 可见范围：公开、仅圈内；首期仅保存选择，不实现权限隔离。
- 图片：支持选择图片；首期可使用本地占位图或临时预览。

**活动附加字段**：活动日期、开始时间、地点、报名人数上限。

**二手附加字段**：价格、成色、交易方式（面交/邮寄）。

**提交规则**：字段校验通过后，将新内容写入本地数据；返回对应社区圈层后可立即看到该内容。

### 5.5 文化旅行

**目标**：将用户的文化兴趣转化为可收藏、可执行的短途路线。

**输入项**：

- 目的地。
- 出行天数：1-3 天、4-7 天、7 天以上。
- 出发时间。
- 预算区间。
- 偏好：文化、美食、自然、艺术，可多选。
- 同行人，可选。

**输出项**：

- 推荐路线列表，每条展示路线名称、途经地点、亮点、价格和可加入操作。
- “生成我的路线”根据输入条件筛选或组合本地预置路线；首期不接入真实 AI 规划服务。
- “加入”后，路线进入个人主页的“路线/计划”列表；再次操作可取消加入。

### 5.6 我的主页

**目标**：呈现用户文化身份和内容资产。

**页面组成**：

- 用户封面、头像、昵称、认证标识、学校/年级、个人简介。
- 身份徽章：文化大使、接待国际友人、优质创作者等。
- 数据概览：关注、粉丝、获赞。
- 内容标签页：笔记、收藏、路线。

**数据关联**：

- 笔记：用户发布的内容与演示预置笔记。
- 收藏：用户在内容详情或列表收藏的内容；首期需新增收藏操作。
- 路线：用户加入的旅行路线和已报名活动。

### 5.7 校园小C

**目标**：提供校园生活服务的统一问答与快捷入口。

**页面组成**：

- 校园活动预告横向卡片。
- 聊天消息列表。
- 快捷问题：查课表、失物招领、找搭子、美食推荐。
- 文本输入、发送、语音和添加附件入口。

**首期行为**：

- 对快捷问题和用户输入返回预置模拟回复。
- 不接入真实大模型、校园教务、天气、图书馆或失物数据库。
- 发送的对话仅保存在本地会话中，重新进入页面后可按实现方案选择保留或清空。

## 6. 关键流程

### 6.1 发布到社区

首页/社区点击发布 -> 选择发布类型 -> 填写内容与圈层 -> 校验 -> 发布成功 -> 返回对应社区圈层并展示新内容。

### 6.2 参与活动

社区/校园圈浏览活动 -> 点击报名 -> 状态变为“已报名” -> 在“我的-路线/计划”中可查看。

### 6.3 生成文化旅行路线

旅行页填写偏好 -> 生成本地推荐结果 -> 加入某条路线 -> 在“我的-路线/计划”中查看或取消。

### 6.4 使用校园小C

首页/校园圈进入校园小C -> 选择快捷服务或输入问题 -> 展示模拟答复 -> 可继续追问。

## 7. 数据与状态边界

首期由后端提供预置种子数据，前端通过 API 获取和提交业务数据；客户端仅保存登录态、必要的界面缓存和草稿。需要统一管理以下实体：

| 实体 | 核心字段 | 首期数据来源 |
| --- | --- | --- |
| 用户 | id、昵称、头像、学校、简介、徽章、统计 | 后端种子数据与微信登录用户 |
| 内容动态 | id、圈层、类型、作者、标题、正文、图片、标签、点赞、收藏、时间 | 后端种子数据 + 用户发布 |
| 活动 | id、关联动态、日期、地点、名额、报名状态 | 后端种子数据 + 用户创建 |
| 二手物品 | id、关联动态、价格、成色、交易方式、意向状态 | 后端种子数据 + 用户创建 |
| 旅行路线 | id、目的地、天数、预算、偏好、站点、亮点、价格、加入状态 | 后端预置与生成结果 |
| 对话消息 | id、角色、内容、时间 | 后端保存的会话记录 |

首期明确不做：支付、即时消息、多人评论、真实地图导航、真实 AI 与校园系统接口。这些能力会在后端和合规方案明确后单独接入。首期包含基础运营管理后台与审核能力。普通用户采用微信登录的基础用户身份，不另行建设账号密码注册。

## 8. 技术架构与前后端边界

### 8.1 默认技术方案

| 层级 | 方案 | 职责 |
| --- | --- | --- |
| 小程序前端 | 微信原生小程序 | 原生页面、组件、路由、交互、微信授权、图片选择与 API 调用 |
| 服务端 | Node.js + TypeScript + NestJS | REST API、身份校验、业务规则、文件上传签名、统一错误处理 |
| 数据库 | MySQL | 用户、内容、活动、报名、收藏、路线和会话等持久化数据 |
| 缓存与会话 | Redis | 登录会话、短期缓存、限流和验证码等可选能力 |
| 文件存储 | 对象存储 | 用户上传图片和内容封面；开发环境可使用本地文件存储适配层 |
| 管理后台 | React + TypeScript + Ant Design | 管理员登录、内容审核、用户与活动管理、运营数据和调试工具 |

默认按单体服务实现，但模块边界独立，未来可以单独拆分内容、活动、旅行和 AI 服务。

### 8.2 前端工程职责

前端使用微信开发者工具可直接打开的原生小程序工程，采用 `pages`、`components`、`services`、`store`、`utils`、`assets` 的清晰目录结构。

- `pages`：页面容器和路由参数处理，不直接承载复杂业务逻辑。
- `components`：内容卡片、活动卡片、导航、筛选栏、空状态、表单字段等可复用组件。
- `services`：按资源封装 API 调用，例如内容、活动、旅行、用户、助手。
- `store`：只存储当前用户、全局未读数、页面间共享的短期状态；业务真相以服务端返回为准。
- `utils`：请求拦截、错误提示、时间格式化、输入校验等公共能力。

前端不得直接写入演示数据作为业务结果。发布、点赞、收藏、报名、路线加入和对话均须通过 API 变更，再以服务端返回状态刷新页面。

### 8.3 后端工程职责

服务端按领域模块划分：认证与用户、内容、社区互动、活动、二手、旅行、文件、校园小C。

- 提供版本化 REST API，例如 `/api/v1/posts`、`/api/v1/events`、`/api/v1/routes`。
- 使用微信 `code` 换取用户身份，服务端签发自身访问令牌；不暴露小程序密钥。
- 对发布、点赞、收藏、报名和路线加入执行登录校验、参数校验、幂等控制和权限校验。
- 通过数据库迁移和种子数据初始化开发、演示环境，避免依赖前端内置业务数据。
- 文件上传采用“前端向后端申请上传凭证，再直传对象存储”的模式；开发阶段可由后端接收上传文件。

### 8.4 用户登录与身份体系

小程序用户不采用传统的“账号 + 密码”注册流程，而是遵循微信小程序的身份模式：

1. 用户首次进入时点击“微信登录”。
2. 小程序获取临时登录凭证并发送给服务端。
3. 服务端调用微信登录接口获取用户唯一标识，自动创建或读取 Weculture 用户记录，并签发访问令牌。
4. 用户首次登录后进入资料补全页，填写昵称、头像、学校和年级；其中昵称、头像可后续修改。
5. 后续打开小程序自动续用登录态；用户可在“我的”页面退出登录。

该方案对用户而言等同于注册，但没有额外密码负担；服务端仍拥有独立用户记录、状态字段和权限控制。手机号、实名信息与校园认证不纳入首期，后续按业务与合规要求扩展。

管理员与普通用户使用不同的身份入口。管理员在管理后台以账号密码登录，服务端使用角色权限控制（RBAC）限制后台接口；默认角色包括超级管理员、运营管理员和内容审核员。

### 8.5 管理后台

管理后台作为独立 Web 工程部署，与小程序共用后端 API，但采用单独的管理员认证和权限体系。它是运营与调试工具，不面向普通用户。

**首期模块**：

| 模块 | 功能 |
| --- | --- |
| 工作台 | 内容、用户、活动、报名和待审核数量概览；近期错误与操作记录摘要 |
| 用户管理 | 查询用户、查看资料和状态、禁用/恢复账号、调整用户标签 |
| 内容管理 | 按圈层、类型、作者和状态检索；查看、审核通过、驳回、下架内容 |
| 活动与二手管理 | 查看、编辑、下架活动和二手信息；查看报名或意向记录 |
| 旅行路线管理 | 维护预置路线、标签、价格、站点和上架状态 |
| 小C知识库 | 管理快捷问题与预置答复，便于不改代码地调整演示内容 |
| 素材管理 | 查看已上传图片、删除无效素材、查看存储引用关系 |
| 系统调试 | 查看 API 请求日志、错误日志、操作审计记录和当前环境健康状态 |
| 管理员与角色 | 管理后台账号、角色和权限；仅超级管理员可操作 |

**审核状态**：用户发布的动态、活动和二手内容均含 `pending`、`approved`、`rejected`、`offline` 状态。首期可配置为“发布后自动通过”以便演示，但后台必须能切换为人工审核流程。

**调试边界**：后台可查看必要的请求信息、错误信息和业务操作审计，但不得展示微信会话密钥、访问令牌、数据库密码等敏感信息。

### 8.6 数据流与错误处理

用户操作 -> 小程序页面调用 `services` -> HTTPS API -> 服务层与数据库 -> 标准化响应 -> 前端刷新状态并反馈结果。

API 统一返回业务状态码、用户可见提示和请求追踪标识。网络异常、无权限、参数错误、资源不存在和服务异常均需在前端展示一致的反馈，不能以静默失败代替。

### 8.7 环境与安全基线

- 划分开发、测试、生产环境，并通过环境变量维护 API 地址、数据库和对象存储配置。
- 密钥、数据库密码和微信凭据只放在服务端环境变量，不提交到代码库。
- 所有写操作要求有效登录态；服务端校验资源归属，禁止客户端仅通过传入用户 ID 获得权限。
- 图片上传限制格式、大小和数量；发布内容预留审核状态字段。
- 接口使用分页、频率限制和参数白名单，防止列表过载与重复提交。
- 管理员操作须记录操作人、时间、对象、动作和结果；高风险操作（禁用用户、下架内容、删除素材）需要二次确认。

## 9. 视觉与交互原则

- 延续 demo 的克制、温和、偏文化编辑感的方向，但在小程序中统一采用中文系统字体，避免依赖网页字体。
- 主色建议沿用暖陶土色 `#C5532E`，搭配暖灰白背景 `#F7F6F3`、深色正文和低对比边框；以图片内容为视觉重点。
- 使用 8px 间距体系，卡片采用轻圆角、细边框和极弱阴影。
- 全站统一导航、顶部栏、空状态、加载状态、操作反馈与底部安全区。
- 图片、头像、图标统一由本地静态资源或小程序图标方案提供，不依赖页面临时外链。

## 10. 建议的实现范围

### 第一阶段：前后端与管理后台可演示闭环

- 原生微信小程序、NestJS API、React 管理后台三个独立工程，提供本地联调说明。
- 五栏主导航与七个页面：首页、社区、发布、旅行、我的、内容详情、校园小C。
- MySQL 数据模型、数据库迁移、种子数据、微信登录与资料补全、普通用户和管理员双身份认证。
- 内容浏览、圈层切换、搜索、点赞、收藏、活动报名、发布、路线生成与加入，全部通过 API 闭环。
- 管理后台的内容审核、用户/活动/路线管理、小C答复配置、操作审计和基础运行调试。
- 基础表单校验、上传适配、空状态、加载反馈、错误提示和接口异常处理。

### 第二阶段：体验与运营完善

- 内容详情的评论列表与评论发布。
- 发布图片的本地选择与预览。
- 个人资料编辑。
- 社区筛选、搜索结果页、通知消息中心。
- 校园小C的会话历史和更完整的模拟知识库。
- 数据统计看板、批量审核、管理员通知与内容审核策略。

### 第三阶段：扩展服务接入

- 用户登录、云端数据和内容审核。
- 地图定位、活动报名名额、消息通知与分享。
- 校园系统、天气、图书馆等数据接口。
- 经审批的 AI 对话服务与安全策略。

## 11. 已确认的实施决策

以下为本次确认后的默认实施决策：

1. 使用微信原生小程序，不使用跨端框架。
2. 建设独立后端、数据库和管理后台，采用前后端分离架构，不以本地模拟数据替代服务端业务逻辑。
3. 默认使用 Node.js、TypeScript、NestJS、MySQL、Redis 与对象存储；若部署环境受限，可在实现时替换基础设施，不改变接口边界。
4. 普通用户使用微信登录自动创建身份，并在首次登录后补全资料；不采用账号密码注册。
5. 管理员使用独立后台账号登录，首期提供工作台、内容审核、用户、活动、路线、小C知识库、素材、日志和角色管理。
6. 校园圈专题页保留为二级页，并将核心内容同时收敛进社区-校园圈。
7. “我的-路线”升级为“我的计划”，同时展示旅行路线和已报名活动。
8. 沿用 Weculture 名称与暖陶土色视觉方向，但会在新工程中统一组件和样式规范。

## 12. 验收标准

当以下事项均可在小程序中完成时，第一阶段即可验收：

- 五个底部导航页面可正常切换，页面状态和导航高亮一致。
- 小程序与服务端可独立启动，前端所有业务数据均由 API 返回；数据库初始化后可获得可用演示数据。
- 微信登录身份能在服务端创建或读取当前用户，首次登录完成资料补全，写操作未登录时被正确拦截。
- 管理后台可独立登录，且不同管理员角色只能访问被授权的功能。
- 管理员可审核、下架和检索内容，管理用户、活动、路线与小C预置答复；关键操作可在审计记录中追溯。
- 首页、社区、旅行、个人主页和校园小C均有稳定的空态/默认态与可用操作。
- 用户可发布一条动态，并能在目标圈层看到它。
- 用户可对内容点赞、收藏，并能在个人主页查看收藏。
- 用户可报名活动，且报名状态能进入“我的计划”。
- 用户可根据条件生成路线、加入路线，并能在“我的计划”中查看。
- 校园小C的快捷入口和文本输入均能得到模拟答复。
- 在常见手机尺寸下，底部导航和输入区不遮挡内容，表单与图片不溢出。
