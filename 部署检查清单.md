# Weculture 部署检查清单

## 1. API 与 MySQL

1. 创建独立 MySQL 8 数据库和最小权限账号，字符集使用 `utf8mb4`。
2. 从 `weculture-api/.env.example` 配置生产环境变量：`NODE_ENV=production`、`DB_TYPE=mysql`、MySQL 连接、HTTPS 后台域名 `CORS_ORIGINS`、微信 AppID/AppSecret 和随机 `JWT_SECRET`。
3. 默认设置 `SEED_DEMO_DATA=false`。如果需要首次初始化演示数据，必须同时设置三个初始管理员密码和学校邀请码，初始化后立即轮换。
4. 在发布过程执行 `npm ci`、`npm run build`、`npm run migration:run`，再用 `npm start` 启动。多实例部署时只由一个发布任务执行迁移。
5. 反向代理开启 HTTPS，并仅暴露 API 端口。使用 `/api/v1/health` 做存活检查。

## 2. 运营后台

1. 以 `weculture-admin/.env.example` 为模板配置 `VITE_API_BASE_URL=https://<API域名>/api/v1`。
2. 执行 `npm ci` 和 `npm run build`，将 `dist` 部署到 HTTPS 静态站点。
3. 将后台域名加入 API 的 `CORS_ORIGINS`，并检查生产页面不会预填演示账号。

## 3. 微信小程序

1. 在 `weculture-miniprogram/config.js` 中填写 `trial` 和 `release` 的 HTTPS API 地址，或通过微信扩展配置下发 `apiBase`。
2. 在微信公众平台将 API 域名加入 `request` 合法域名，证书链必须有效。
3. 上传前恢复域名校验，用体验版完成微信登录、学校认证、内容发布/审核、活动报名和旅行计划回归。

## 4. 发布前验证

1. 执行 API `npm test` 和 `npm run build`，执行后台 `npm run build`。
2. 对备份恢复和迁移回滚做一次演练；回滚前先备份数据。
3. 确认代码库中没有 `.env`、数据库文件、真实 AppSecret、JWT 密钥或生产密码。
4. 确认日志和监控不记录 token、密码或微信 AppSecret，并为 5xx、迁移失败和数据库连接异常配置告警。
