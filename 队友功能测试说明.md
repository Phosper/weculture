# Weculture 队友功能测试说明

这份说明面向不需要看代码的测试队友。测试人员只需要微信和浏览器。

## 负责人需要先准备

- 部署一套独立的测试 API 和 MySQL，API 必须有公网 HTTPS 域名。
- 将测试 API 域名填入 `weculture-miniprogram/config.js` 的 `trial`。
- 在微信小程序后台将该域名加入 `request` 合法域名。
- 在微信开发者工具上传一个版本，并在小程序后台将它选为体验版。
- 在“成员管理 → 体验成员”中加入队友的微信号，然后把体验二维码发给他们。
- 如果要测运营后台，同时部署测试后台网址，并分配专用的测试审核员账号。不要分享生产超级管理员密码。

`127.0.0.1` 只能在开发电脑上使用，手机体验版无法访问它。

## 发给测试队友的说明

1. 用微信扫描体验版二维码。
2. 首次打开后设置昵称，使用测试邀请码 `BLCU2026` 完成学校认证。
3. 按顺序尝试：首页搜索、社区筛选、详情、点赞、收藏、活动报名、旅行推荐、加入计划、小C问答、发布动态、个人页。
4. 发布动态后告诉负责人标题，由后台测试人员审核，再回到小程序确认内容是否可见。
5. 不要填写真实隐私、联系方式或敏感信息；所有标题加上“测试”字样。

## 反馈格式

每个问题单独记录：

- 手机型号和系统：
- 微信版本：
- 出问题的页面：
- 之前做了哪些操作：
- 实际看到什么：
- 期望看到什么：
- 截图或录屏：
- 是否每次都能复现：

## 建议分工

- 队友 A：首次登录、认证、搜索、社区和发布。
- 队友 B：点赞、收藏、活动报名、旅行计划和个人页。
- 队友 C：校园小C，包含已知问题、未知问题、空输入和网络中断。
- 队友 D：运营后台的登录、审核、邀请码、路线和知识库。

每人使用自己的微信身份，不共用同一测试账号，避免点赞、报名和计划状态互相干扰。
