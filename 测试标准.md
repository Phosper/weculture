# Weculture 阶段一测试标准

> 用途：交给独立测试人员或其他 AI 执行。除“测试环境准备”外，每一项均应记录实际结果、截图或接口响应摘要，并标记通过/失败。

> 当前实现范围：本轮已完成单校认证、审核、报名、路线、小C知识库及其后台闭环。真实微信身份换取、文件直传/对象存储、用户禁用、第二所学校的隔离回归和生产 MySQL 迁移属于部署与下一阶段能力，不应按“本轮失败项”统计。

## 1. 测试环境准备

1. 启动 API：在 `weculture-api` 目录执行 `npm run dev`。
2. 启动管理后台：在 `weculture-admin` 目录执行 `npm run dev`，访问 `http://127.0.0.1:5173`。
3. 用微信开发者工具导入 `weculture-miniprogram`，并确认“本地设置”已允许访问本机 HTTP 开发地址；真机或体验版必须改用已备案的 HTTPS API 域名。
4. 测试邀请码：`BLCU2026`。本地后台账号：`admin` / `Weculture@2026`；学校运营账号：`blcu-operator` / `Weculture@2026`；审核员账号：`blcu-reviewer` / `Weculture@2026`。
5. 每次需要干净数据时，停止 API 并删除 `weculture-api/weculture.sqlite` 后重启；不要在生产环境使用此方式。

## 2. 必须通过的功能测试

| 编号 | 场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| U01 | 未登录访问 | 打开小程序首页 | 引导微信登录；没有令牌时不能调用需登录 API |
| U02 | 首次资料 | 使用新微信测试身份登录 | 可填写昵称；昵称少于 2 字或超过 20 字时不可保存 |
| U03 | 未认证隔离 | 未输入邀请码时进入社区、发布页 | 跳转认证页或提示需认证；直接请求校内内容接口返回无权限 |
| U04 | 学校认证 | 输入 `BLCU2026` | 显示北京语言大学已认证，社区入口解锁 |
| U05 | 无效邀请码 | 输入随机或过期邀请码 | 明确提示邀请码无效，不创建校内成员关系 |
| U06 | 校内内容流 | 认证后进入社区，切换全部/动态/活动/闲置 | 只展示已通过的本校内容；筛选结果正确 |
| U07 | 内容发布 | 分别提交动态、活动、闲置 | 普通内容进入待审核；活动缺少地点、时间、截止时间或名额时不得提交 |
| U07a | 无效活动原子性 | 活动人数传入非数字、0、负数或缺失；再检查“我的投稿” | 返回具体参数错误，且不产生任何待审核帖子或孤立活动记录 |
| U07b | 过期活动 | 开始时间或报名截止时间设为过去 | 返回“时间已过期”类错误，不创建内容 |
| U07c | 重复提交 | 在网络正常与网络延迟条件下连续点击发布 | 客户端提交中禁止再次发起请求；服务端每次有效请求最多创建一条内容 |
| U08 | 审核闭环 | 后台审核通过/驳回一条待审核内容 | 通过后前台可见；驳回后前台不可见，且用户端可读取驳回原因（当前版本预留接口状态，后续增加“我的投稿”页展示） |
| U09 | 点赞收藏 | 对同一内容连续点击点赞和收藏 | 第一次生效，第二次取消；刷新后状态和数量保持一致 |
| U10 | 活动报名 | 打开活动详情并报名，连续点击或重复调用 | 只产生一条有效报名记录，不超额；“我的计划”出现该活动 |
| U11 | 取消报名 | 取消已报名活动 | 活动人数减一，计划标记取消或从有效计划消失 |
| U12 | 旅行路线 | 填写目的地/天数/偏好并生成 | 返回匹配路线；加入和取消加入均同步到“我的计划” |
| U13 | 校园小C | 点击快捷问题并输入已配置关键词与未知问题 | 已配置关键词返回对应答复；未知问题明确说暂未收录，不伪造实时数据 |
| U14 | 我的投稿 | 发布待审核内容后进入“我的” | 可看到审核中/已发布/已驳回状态；驳回内容展示原因，作者可预览自己的待审核内容 |

## 3. 管理后台测试

| 编号 | 场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| A01 | 管理员登录 | 用正确/错误密码登录 | 正确进入工作台；错误不得泄露账号是否存在 |
| A02 | 工作台 | 登录后查看概览 | 待审核、已发布、成员、活动数量与数据库数据一致 |
| A03 | 内容审核 | 处理待审核内容 | 审核通过、驳回、下架均生效；驳回必须填写原因 |
| A04 | 邀请码 | 创建一个有限次数邀请码并使用 | 后台显示批次、次数、有效期；达到上限后无法再兑换 |
| A05 | 路线管理 | 创建路线 | 路线保存后出现在后台和用户旅行推荐中（条件匹配时） |
| A06 | 小C知识库 | 创建关键词和答复 | 新问题能由对应学校用户获得新答复 |
| A07 | 成员管理 | 学校运营账号登录查看成员 | 仅展示本校认证成员；不应出现平台敏感数据 |
| A08 | 审计日志 | 审核内容、创建邀请码/路线/知识 | 工作台近期操作可看到操作类型、资源和时间；日志不能含令牌、微信标识或密码 |
| A09 | 角色权限 | 以审核员角色调用邀请码、路线、知识库管理接口 | 服务端应返回无权限；不能只依靠菜单隐藏 |

## 4. 安全与数据隔离测试

- 无 `Authorization` 请求所有写接口，应返回未登录。
- 已登录未认证用户请求 `/posts?scope=school`、创建内容、报名活动，应返回无学校认证。
- 用户不能通过在 URL 或请求体伪造 `schoolId` 获取其他学校内容；后续多校环境必须用第二所学校数据复测。
- 非管理员令牌请求 `/admin/*`，必须被拒绝；管理员令牌请求用户接口也不能冒充普通用户。
- 前端、后台、日志和错误响应中不得出现微信密钥、访问令牌、密码哈希、数据库连接字符串或完整微信身份标识。

## 5. 体验与兼容性测试

- 在 375px、390px、414px 宽度的手机模拟器检查：首页、社区、发布、旅行、我的、认证、详情、小C没有文字截断、按钮重叠或底部导航遮挡。
- 表单在键盘弹出后仍可提交；长标题、长正文、空图片和网络失败均有合理状态。
- 管理后台在 1440px 桌面与 390px 窄屏中可登录、切换导航、完成审核；表格允许横向滚动或自适应，不遮挡主要操作。
- 网络断开、401、403、404、409、500 分别显示可理解的用户提示，不能静默失败或无限加载。
- 验证 400、401、403、404、409、500 的响应体均含 `code`、`message`、`requestId`；前端在响应体缺失时应至少显示 HTTP 状态提示，不能因读取空对象崩溃。

## 6. 自动自测命令

API 已启动后，在 `weculture-api` 执行：

```powershell
npm run self-test
```

该命令覆盖健康检查、未认证隔离、邀请码认证、内容待审核与通过、活动报名幂等、路线计划和小C答复。它不替代小程序真机测试、后台视觉检查、并发压测或未来多校隔离测试。
