# Weculture 小程序系统设计文档

> 状态：开发基线
>
> 本文档是后续工程实现的唯一设计依据，覆盖并细化《Weculture小程序需求说明》中的技术与范围决策。项目从一所试点学校的内部用户开始，架构预留逐步扩展至多所高校的平台能力。

## 1. 目标与范围

Weculture 是面向高校文化交流与校园生活的微信小程序。首期服务于一所试点学校的认证成员，提供校内文化内容、活动报名、校园闲置、文化旅行计划与校园小C服务；平台管理员可通过后台完成审核、配置和基础运维。

首期目标不是构建开放式社交网络，而是验证校内内容与活动的运营闭环：

`微信登录 -> 学校邀请码认证 -> 浏览/发布内容 -> 后台审核 -> 活动报名或收藏 -> 我的计划`

首期不包含支付、即时私信、多人评论、真实地图导航、真实教务/图书馆接口和真实大模型服务。小C使用学校管理员维护的知识库与预置规则回复。

## 2. 产品与权限边界

### 2.1 可见范围

| 范围 | 内容来源 | 可见用户 | 首期用途 |
| --- | --- | --- | --- |
| 平台精选 | 平台管理员 | 所有已登录用户 | 首页文化内容与旅行推荐 |
| 校内可见 | 已认证校内成员、学校管理员 | 同一学校的认证成员 | 社区动态、活动、校园闲置、小C知识 |
| 跨校可见 | 后续开放 | 指定学校或全部认证成员 | 不在首期启用 |

首期的“同城”和“国际”仅是平台精选内容的主题标签，不允许以此建立面向陌生用户的开放互动圈。社区的互动主场是校内圈。

### 2.2 角色

| 角色 | 所属范围 | 权限 |
| --- | --- | --- |
| 游客 | 无学校成员关系 | 可查看登录前公共说明；不能读取业务内容或执行写操作 |
| 已登录未认证用户 | 平台 | 可查看平台精选、填写资料、完成邀请码认证 |
| 校内成员 | 单个学校 | 浏览与发布本校内容、收藏、报名、管理自己的资料和内容 |
| 内容审核员 | 单个学校 | 审核与下架本校内容，查看审核记录 |
| 运营管理员 | 单个学校 | 管理本校用户、活动、路线、邀请码、小C知识与内容 |
| 平台超级管理员 | 平台 | 管理学校、学校管理员、全局精选内容、全局配置与系统诊断 |

后台权限使用角色权限控制（RBAC）。权限绑定为“管理员 + 角色 + 学校”，而非仅依赖前端菜单隐藏。

## 3. 系统架构

```text
微信原生小程序                 React 管理后台
        |                              |
        +---------- HTTPS API ---------+
                       |
              NestJS 模块化单体服务
                       |
       +---------------+----------------+
       |               |                |
     MySQL        对象存储          Redis（可选）
  业务持久化    图片/附件文件     缓存、限流、会话扩展
```

项目由三个独立工程组成：

| 工程 | 默认技术 | 职责 |
| --- | --- | --- |
| `weculture-miniprogram` | 微信原生小程序 | 用户界面、微信能力、页面交互、API 调用 |
| `weculture-api` | Node.js、TypeScript、NestJS | 认证、权限、业务规则、REST API、数据迁移、种子数据 |
| `weculture-admin` | React、TypeScript、Ant Design | 管理员登录、内容审核、学校运营与系统诊断 |

后端首期采用模块化单体而非微服务。模块之间通过领域接口隔离，未来可按内容、活动、旅行或AI服务拆分，不提前引入分布式复杂度。

## 4. 多学校数据模型

### 4.1 建模原则

- 所有校内资源必须带 `schoolId`，包括内容、活动、闲置、路线、邀请码、知识库和管理员权限。
- 用户与学校的关系独立建模为 `school_memberships`，不在用户表上固化唯一学校。这样可支持未来多校身份、校友身份和管理员跨校管理。
- 每个请求由服务端根据访问令牌解析当前用户与当前学校成员关系；客户端传入的 `schoolId` 仅作筛选条件，不能用于越权访问。
- 管理后台查询也必须自动附加管理员授权的学校范围；只有平台超级管理员可以跨学校操作。

### 4.2 核心实体

| 实体 | 关键字段 | 说明 |
| --- | --- | --- |
| `users` | id、wechatOpenId、昵称、头像、状态、注销时间 | 平台级用户；微信唯一标识仅存服务端 |
| `schools` | id、名称、简称、状态、配置 | 学校租户；首期仅启用一所 |
| `school_memberships` | userId、schoolId、认证状态、成员角色、加入时间 | 用户在学校中的身份关系 |
| `invite_codes` | schoolId、编码、有效期、使用上限、已用次数、状态 | 学校邀请码与使用审计 |
| `posts` | id、schoolId、作者、类型、标题、正文、可见范围、审核状态 | 动态、活动和闲置的共同内容主体 |
| `post_media` | postId、文件Id、排序 | 内容图片关系 |
| `events` | postId、开始/结束时间、地点、名额、报名截止时间、状态 | 活动附加信息 |
| `event_registrations` | eventId、userId、状态、操作时间 | 报名记录；唯一约束防止重复报名 |
| `market_items` | postId、价格、成色、交付方式、意向状态 | 校园闲置附加信息 |
| `favorites` | userId、目标类型、目标Id | 内容与路线收藏关系 |
| `travel_routes` | schoolId、范围、名称、条件、站点、价格、状态 | 平台精选或校内路线 |
| `user_plans` | userId、目标类型、目标Id、状态 | 用户加入的路线与已报名活动 |
| `assistant_knowledge` | schoolId、问题、关键词、答复、状态 | 小C预置问答，按学校隔离 |
| `files` | 归属、存储键、MIME、大小、状态 | 图片与附件元数据 |
| `admin_accounts` | 用户名、密码哈希、状态 | 管理员独立登录身份 |
| `admin_role_bindings` | adminId、roleId、schoolId | 管理员角色与学校范围 |
| `audit_logs` | 操作人、学校、资源、动作、结果、时间 | 管理操作可追溯记录 |

## 5. 认证与账号流程

### 5.1 小程序用户

1. 用户点击微信登录，小程序将临时登录凭证发送至后端。
2. 后端通过微信服务换取身份标识，创建或读取 `users` 记录。
3. 后端签发短期访问令牌和可续期令牌；小程序安全保存并自动续期。
4. 用户首次登录补全昵称、头像、学校。头像使用小程序提供的选择能力，昵称使用受控输入，不依赖废弃的网页式用户资料授权接口。
5. 用户输入有效邀请码后，后端创建或更新 `school_memberships`，状态变为 `verified`。
6. 用户可在“我的”查看认证学校、退出登录、申请删除账号与个人内容。

微信标识、访问令牌、密钥与会话信息不返回或展示给管理后台。手机号、身份证和学籍数据不在首期收集。

### 5.2 邀请码

- 由学校运营管理员在后台批量生成，支持命名批次、有效期、最大使用次数和手动失效。
- 每次兑换记录用户、学校、时间和邀请码批次；邀请码原始值只在创建/导出时展示。
- 首期邀请码是唯一的校园认证方式。未来若获取学校官方认证能力，可增加认证适配器，不改变成员关系模型。

### 5.3 管理员

管理员通过后台账号密码登录，密码只保存强哈希。首次超级管理员由部署环境变量或初始化命令创建，后台不提供公开注册入口。登录、退出、密码重置和高风险操作须写入审计日志。

## 6. 业务规则

### 6.1 内容发布与审核

内容状态统一为：

`draft -> pending -> approved / rejected -> offline`

- 校内成员可创建草稿和提交待审核内容；没有认证学校时不能发布校内内容。
- 审核通过后才进入校内信息流；驳回必须附带用户可见原因。
- 审核员可下架违规内容；用户可删除自己的草稿或已发布内容，删除采用软删除并记录审计。
- 开发与演示环境可启用“自动通过”配置，但生产环境默认人工审核。
- 发布时校验文本长度、图片格式、图片大小、图片数量与可见范围；为后续接入文本/图片安全审核预留字段和回调状态。

### 6.2 点赞、收藏与计划

- 点赞和收藏均为用户与目标资源的一对一关系，数据库使用唯一约束保证幂等。
- 点赞计数可异步汇总，但用户的当前状态必须以关系表为准。
- 用户加入旅行路线后创建 `user_plans`；取消加入仅修改本人的计划状态。
- “我的计划”展示已加入路线和有效报名活动；“我的收藏”展示已收藏内容与路线。

### 6.3 活动报名

- 活动必须具备开始时间、结束时间、地点、报名截止时间和名额上限；所有字段在审核前完成校验。
- 报名接口在数据库事务内执行，使用唯一约束防止重复报名，并锁定或原子更新可用名额，避免超卖。
- 截止、满员、取消和活动下架时拒绝新报名；用户取消报名后释放名额。
- 运营管理员可查看本校活动报名名单；审核员无权导出或管理名单。
- 活动改期、取消或下架后，已报名用户的计划标记为“已变更/已取消”，首期通过小程序内状态提示告知，后续再接入订阅消息。

### 6.4 校园闲置

首期定位为“校内闲置信息”，不是交易平台。用户可发布、浏览、收藏和提交意向，不提供支付、私信、公开手机号或担保交易。真实撮合与沟通能力待消息和安全机制完善后再开放。

### 6.5 文化旅行

旅行页依据目的地、出行天数、预算、偏好和同行人，从服务端预置路线中筛选或组合结果。首期不调用真实AI规划或地图服务；路线由运营管理员维护并可按校内或平台精选范围上架。

### 6.6 校园小C

- 小C先检索当前学校启用的知识库，再检索平台通用知识库。
- 命中快捷问题或关键词时返回配置的答复；未命中时明确说明“暂未收录”，不编造实时教务、天气、图书馆或失物信息。
- 会话记录按用户和学校保存，用户可清空自己的会话。
- 学校运营管理员管理本校问答；平台管理员管理通用文化问答。

## 7. 小程序页面设计

### 7.1 导航与页面

| 页面 | 路径职责 | 校内认证要求 |
| --- | --- | --- |
| 首页 | 平台精选、本校精选、活动入口、小C入口 | 登录后可见；校内模块要求认证 |
| 社区 | 本校最新动态、分类筛选、活动与闲置 | 必须认证 |
| 发布 | 动态、活动、闲置三类表单 | 必须认证 |
| 旅行 | 偏好填写、路线推荐、加入计划 | 登录后可见；校内路线要求认证 |
| 我的 | 资料、学校认证、笔记、收藏、我的计划、设置 | 登录后可见 |
| 内容详情 | 内容、图片、点赞、收藏、活动操作 | 依内容范围决定 |
| 校园小C | 校园活动预告、快捷问题、会话 | 登录后可见；校内知识要求认证 |

小程序采用五栏底部导航：首页、社区、发布、旅行、我的。校园小C、内容详情、学校认证和校园圈分类为二级页面。

### 7.2 前端工程边界

```text
weculture-miniprogram/
  pages/          页面容器与页面配置
  components/     可复用业务与基础组件
  services/       API 资源封装
  store/          当前用户、认证状态、轻量全局状态
  utils/          请求、校验、格式化、错误处理
  assets/         本地图片、图标和字体资源
```

页面不得直接写入业务数据。发布、点赞、收藏、报名、邀请码兑换、路线加入和对话均经 `services` 调用后端 API，并以服务端返回值更新界面。客户端只缓存令牌、用户摘要、草稿与非关键页面缓存。

## 8. REST API 边界

接口统一使用 `/api/v1` 前缀、HTTPS、JSON 响应、分页参数和请求追踪 ID。写操作需携带访问令牌并通过服务端权限校验。

| 领域 | 主要接口 | 说明 |
| --- | --- | --- |
| 认证 | `POST /auth/wechat/login`、`POST /auth/refresh`、`POST /auth/logout` | 微信登录、令牌续期、退出 |
| 当前用户 | `GET/PATCH /me`、`POST /me/school-verifications`、`DELETE /me` | 资料、邀请码认证、注销 |
| 内容 | `GET/POST /posts`、`GET /posts/:id`、`POST /posts/:id/likes`、`POST /posts/:id/favorite` | 浏览、发布、互动 |
| 活动 | `POST /events/:id/registrations`、`DELETE /events/:id/registrations/me` | 报名与取消 |
| 闲置 | `POST /market-items/:id/interests` | 意向登记 |
| 旅行 | `POST /travel-routes/recommendations`、`POST /travel-routes/:id/plans` | 推荐与加入计划 |
| 小C | `GET /assistant/quick-actions`、`POST /assistant/messages` | 快捷问题与对话 |
| 文件 | `POST /files/upload-credentials`、`POST /files/complete` | 获取上传凭证与确认上传 |
| 管理后台 | `/admin/*` | 管理员专用认证和资源管理接口 |

后端统一返回业务码、可显示消息、数据和 `requestId`。错误码至少区分未登录、无学校认证、无权限、参数错误、资源不存在、状态冲突、上传失败和服务异常。

## 9. 管理后台设计

### 9.1 首期模块

| 模块 | 学校运营管理员 | 平台超级管理员 |
| --- | --- | --- |
| 工作台 | 本校待审核、用户、活动、错误摘要 | 全平台概览与学校状态 |
| 内容审核 | 审核、驳回、下架本校内容 | 管理全局精选与跨校内容 |
| 用户与认证 | 查询本校成员、禁用/恢复、邀请码批次 | 管理学校与学校管理员 |
| 活动与路线 | 管理本校活动、报名名单、路线 | 管理平台精选路线 |
| 校园闲置 | 管理本校闲置内容和意向 | 全局风险处置 |
| 小C知识库 | 管理本校知识与快捷问题 | 管理通用知识库 |
| 素材管理 | 查看本校文件引用，清理无效素材 | 管理全局存储策略 |
| 系统诊断 | 无 | 查看脱敏错误、接口健康、操作审计 |
| 管理员与角色 | 无 | 创建管理员、绑定学校和角色 |

### 9.2 管理与调试安全规则

- 后台只展示必要、脱敏的请求日志和错误摘要，禁止展示令牌、微信身份标识、密钥和数据库连接信息。
- 禁用用户、下架内容、删除素材和调整管理员角色属于高风险操作，必须二次确认并写入审计日志。
- 管理员不能查看未授权学校的数据，前端筛选条件不能替代服务端的学校范围校验。
- 图片删除前检查是否仍被业务资源引用；无引用文件采用延迟清理，避免误删已发布内容。

## 10. 文件与数据安全

- 小程序仅上传符合白名单的图片格式，限制单文件大小、单次数量和总配额。
- 文件存储使用私有对象与短期访问地址；后台和小程序不直接暴露存储密钥。
- 生产环境采用数据库迁移管理结构变更，并执行定期备份、恢复演练和监控告警。
- 区分开发、测试、生产环境；微信凭据、数据库密码、管理员初始密码与对象存储密钥仅存在部署环境变量中。
- 用户注销后立即失效登录态，个人资料与未公开内容按数据保留策略软删除或匿名化；文件按引用关系延迟清理。

## 11. 开发阶段与验收

### 阶段一：单校可运营内测

- 三个工程可独立启动并完成本地联调。
- 一所学校配置、学校管理员、邀请码和认证校内成员可用。
- 内容发布到审核、审核通过到信息流、活动报名到我的计划完整闭环。
- 平台精选内容、校内社区、校园闲置、旅行规则推荐与小C知识库可用。
- 后台具备内容审核、用户/邀请码、活动/路线、小C知识、基础日志与审计能力。

### 阶段二：多校试点

- 启用多所学校，所有校内数据与管理员权限按学校隔离。
- 允许学校独立维护路线、活动、知识库与邀请码批次。
- 增加数据统计、批量审核、订阅消息与更完整的运营配置。

### 阶段三：平台开放

- 设计可控的跨校内容、同城活动与国际交流范围。
- 评估接入校园系统、地图、支付、消息和受审批AI服务。
- 建立平台级内容安全、风控、申诉与服务等级保障。

### 阶段一验收标准

- 未认证用户无法读取或发布任何校内数据，认证成员无法越权读取其他学校数据。
- 邀请码的有效期、使用次数、作废和兑换记录均由服务端正确处理。
- 管理员审核内容后，用户端列表按审核状态正确展示；驳回原因可见。
- 活动不会因重复请求超额报名；取消报名可释放名额；改期或取消会在我的计划中标记。
- 用户操作全部通过 API 持久化，刷新小程序或后台后状态仍一致。
- 管理后台的权限边界、操作审计和敏感信息脱敏可验证。
- API、数据库迁移、种子数据和错误处理具备自动化测试；常见手机尺寸下页面不遮挡、不溢出。

## 12. 默认决策清单

| 决策 | 默认值 |
| --- | --- |
| 首期市场 | 一所学校的内部用户 |
| 用户认证 | 微信登录 + 学校邀请码 + 首次资料补全 |
| 数据架构 | 共享数据库、行级 `schoolId` 隔离、成员关系表 |
| 后端 | NestJS 模块化单体 + MySQL；Redis 可选 |
| 管理后台 | React + Ant Design，独立管理员认证与RBAC |
| 内容审核 | 生产默认人工审核；开发/演示可自动通过 |
| 校园闲置 | 信息与意向登记，不提供交易、支付或私信 |
| 旅行与小C | 服务端规则与可配置知识库，不接真实AI或校园数据接口 |
| 多校扩展 | 阶段二启用，首期数据库与权限模型已兼容 |

